script:
  - mkdir build
  - echo "Testing gh-pages" > build/index.html

deploy:
  edge:
    source: webknjaz/dpl
    branch: feature/34-gh-pages-deployment
  provider: pages
  skip-cleanup: true
  github-token: xxx
  local-dir: build
  on:
    branch: master

notifications:
  webhooks: "http://requestb.in/zqdupszq"
